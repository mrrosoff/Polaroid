#pragma once

// Copy to Secrets.h and fill in. Secrets.h is gitignored.
//
// Matches the SSM parameter /website/polaroid/device-secret. The Spotify
// display authenticates the same way, against /website/spotify/device-secret.

#define POLAROID_DEVICE_TOKEN "replace-me"
