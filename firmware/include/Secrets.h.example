#pragma once

// Copy to Secrets.h and fill in. Secrets.h is gitignored.
//
// The device token is the value stored in SSM at /polaroid/device-token.
// Rotating it means reflashing, which is fine — one device, one owner.

#define POLAROID_DEVICE_TOKEN "replace-me"
